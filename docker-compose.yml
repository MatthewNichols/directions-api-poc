services:
  directions-api-poc:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '${PORT:-3001}:3001'
    environment:
      NODE_ENV: production
      PORT: 3001
      ORS_API_KEY: ${ORS_API_KEY}
    restart: unless-stopped
